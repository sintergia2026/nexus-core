# Absolute path to the persisted records directory.
# If not set, each service falls back to its own __dirname or process.cwd() relative default.
# Required for deployed environments where the default relative path does not resolve correctly.
# NEXUS_RECORDS_DIR=/absolute/path/to/10_examples/persisted_records
